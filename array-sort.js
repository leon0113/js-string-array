const numbers = [1, 9, 2, 5, 7, 3, 8, 50, 20];
const sorted = numbers.sort(function (a, b) {
    return a - b;
});
// console.log(sorted);

// reverse
const reversed = numbers.reverse();
console.log(reversed);